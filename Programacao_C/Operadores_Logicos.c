#include <stdio.h>

/**
 * Script de validacao de lógica Booleana
 * Objetivo: Testar o operador && (AND) conforme mapeamento da tabela verdade.
 */

//int main() {
    // Definicao das entradas conforme a imagem
    //int entradas_A[] = {1, 1, 0, 0};
    //int entradas_B[] = {1, 0, 1, 0};
    //int total_casos = 4;

    //printf("Iniciando Teste de Logica: Operador && (AND)\n");
    //printf("--------------------------------------------\n");
    //printf("|  A  |  B  |  A && B (Resultado) |\n");
    //printf("--------------------------------------------\n");

    //for (int i = 0; i < total_casos; i++) {
        //int a = entradas_A[i];
        //int b = entradas_B[i];
        
        // Aplicacao do operador logico
        //int resultado = (a && b);

        // Saida formatada para conferencia
        //printf("|  %d  |  %d  |          %d          |\n", a, b, resultado);
    //}

    //printf("--------------------------------------------\n");
    //printf("Teste concluido com sucesso.\n");

    //return 0;
//}
#include <stdio.h>

/**
 * Script de validação de lógica Booleana - Operador OR (||)
 * Fonte -> Protocolo (||) -> Transformação -> Destino
 */

//int main() {
    // Fonte: Definição dos estados lógicos de entrada
    //int entradas_A[] = {1, 1, 0, 0};
    //int entradas_B[] = {1, 0, 1, 0};
    //int total_casos = 4;

   // //printf("Iniciando Teste de Logica: Operador || (OR)\n");
    //printf("--------------------------------------------\n");
    //printf("|  A  |  B  |  A || B (Resultado) |\n");
    //printf("--------------------------------------------\n");

    //for (int i = 0; i < total_casos; i++) {
        //int a = entradas_A[i];
        //int b = entradas_B[i];
        
        // Protocolo/Transformação: Aplicação do operador OU lógico
        //int resultado = (a || b);

        // Destino: Saída formatada para conferência técnica
        //printf("|  %d  |  %d  |          %d          |\n", a, b, resultado);
    //}

    //printf("--------------------------------------------\n");
    //printf("Validacao de redundancia concluida.\n");

    //return 0;
//}
#include <stdio.h>

/**
 * Script de validação de lógica Booleana - Operador NOT (!)
 * Fonte -> Protocolo (!) -> Transformação -> Destino
 */

//int main() {
    // Fonte: Definição dos estados lógicos de entrada (apenas uma variável)
    //int entradas[] = {1, 0};
    //int total_casos = 2;

    //printf("Iniciando Teste de Logica: Operador ! (NOT)\n");
    //printf("--------------------------------------------\n");
    //printf("|  A  |  !A (Resultado) |\n");
    //printf("--------------------------------------------\n");

    //for (int i = 0; i < total_casos; i++) {
        //int a = entradas[i];
        
        // Protocolo/Transformação: Inversão do estado lógico
        // Em C, !1 vira 0 e !0 vira 1
        //int resultado = !a;

        // Destino: Saída formatada
        //printf("|  %d  |        %d        |\n", a, resultado);
    //}

    //printf("--------------------------------------------\n");
    //printf("Inversao de sinal validada.\n");

    //return 0;
//}