#include <stdio.h>

//int main() {
        // Definicao dos estados logicos de entrada
       //int a = 10, b = 5;
       
       //if (a > 0 && b > 0) {
           //printf("Ambos A e B sao positivos.\n");
       //} else {
           //printf("Pelo menos um dos numeros nao e positivo.\n");
       //}

    //return 0;
//
//}
//int main() {
        // Definicao dos estados logicos de entrada
       //int a = 10, b = 5;
       
       //if (a > 0 || b > 0) {
           //printf("Pelo menos um dos numeros e positivo.\n");
       //} else {
           //printf("Os dois numeros são negativos.\n");
       //}

    //return 0;
//}
//int main() {
        
       //int a = 5, b = -10, c =1;
       
       // a > 0 -> Verdadeiro
       // a < 0 -> Verdadeiro
       // Verdadeiro || Verdadeiro -> Verdadeiro
       // Verdadeiro && c = 0
       // Verdadeiro || Falso -> Verdadeiro

              //if (a > 0 && b < 0 || c == 0) {
                 //printf("A condição é verdadeira\n");
              //} else {
                 //printf("A condição é falsa\n");
//}
//}
    //return 0;
//}