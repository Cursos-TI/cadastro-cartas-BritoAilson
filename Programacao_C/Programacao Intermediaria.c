#include <stdio.h>

int main () {
//** Estudo da Estrutura de Decisão estrutura Sequencial**/

    //int maca, laranja;

   // printf("Digite a quantidade de maças: ");
   // scanf("%d", &maca);
    
   // printf("Digite a quantidade de laranjas: ");
   // scanf("%d", &laranja);


   // if (maca <= laranja ) {

       // printf("Tem mais laranjas do que maças.\n");

   // } else if (maca < 0) {
      //  printf("Tem um numero negativo de maças, o que e invalido.\n");
   // } else if (laranja <= maca) {
     //   printf("Tem mais maças do que laranjas.\n");
    //} else {
      ///  printf("Tem a mesma quantidade de maças e laranjas.\n");
   // }

   // return 0;

   







}